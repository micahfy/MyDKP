<view class="section card">
  <view>管理员登录</view>
  <input placeholder="用户名" value="{{username}}" bindinput="onUserChange" />
  <input placeholder="密码" password value="{{password}}" bindinput="onPassChange" />
  <button class="btn" bindtap="onLogin" loading="{{loading}}">登录</button>
</view>
